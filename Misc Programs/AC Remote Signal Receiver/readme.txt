Point Remote at IR receiver and press buttons on remote to record codes when the sketch is running.

Most AC remotes operate at 38 kHz but yours could be different.

Remove all instances of "-" from scanned codes before adding them to code array.

Update AC code array size wherever necessary.